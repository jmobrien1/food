# Database
POSTGRES_USER=chefdecuisine
POSTGRES_PASSWORD=changeme
POSTGRES_DB=chefdecuisine
DATABASE_URL=postgresql+asyncpg://chefdecuisine:changeme@db:5432/chefdecuisine

# LLM — set to a claude model (e.g. claude-sonnet-4-6) to use Anthropic,
# or any other name (e.g. qwen2.5) to use the local Ollama instance.
LLM_MODEL=qwen2.5

# Anthropic (only needed when LLM_MODEL contains "claude")
ANTHROPIC_API_KEY=sk-ant-your-key-here

# Ollama (only needed when LLM_MODEL does NOT contain "claude")
OLLAMA_BASE_URL=http://host.docker.internal:11434

# Backend
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000
CORS_ORIGINS=["http://localhost:3004","http://10.0.0.5:3004"]

# Host ports (mapped from container ports)
API_HOST_PORT=8004
WEB_HOST_PORT=3004

# Frontend
NEXT_PUBLIC_API_URL=http://10.0.0.5:8004

# Embedding
EMBEDDING_MODEL=all-MiniLM-L6-v2
EMBEDDING_DIM=384

# pgAdmin (debug only)
PGADMIN_EMAIL=admin@chef.local
PGADMIN_PASSWORD=admin
